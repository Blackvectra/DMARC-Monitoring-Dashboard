# .NET build output
src/**/bin/
src/**/obj/
*.user

# Local working directories (reports, state, logs) must never be committed:
# they contain customer mail metadata.
WorkingDir/
Reports/
State/
Logs/

# A database created by running a command in the repo, not part of the source.
dmarc.db
dmarc.db-wal
dmarc.db-shm
